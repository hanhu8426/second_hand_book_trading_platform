<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.secondhandbookstore.mapper.UserMapper">

    <insert id="addAddressList" useGeneratedKeys="true" keyProperty="addId" keyColumn="addId">
            insert into address(name,phone,address,id,area)
            values(#{name},#{phone},#{address},#{id},#{area})
    </insert>

    <update id="modifyUserInfo">
        update user
        <set>
            <if test="img!=null">img = #{img},</if>
            <if test="phone!=null">phone = #{phone},</if>
            <if test="area!=null">area = #{area},</if>
            <if test="gender!=null">gender = #{gender},</if>
            <if test="introduce!=null">introduce = #{introduce},</if>
        </set>
        where id = #{id}
    </update>

</mapper>